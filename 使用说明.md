# Cursor Cult · 小游戏改造副本

原项目来自 `D:\code\cult`。所有修改均在此副本完成。

## 启动

电脑已安装 Node.js 时，在本目录运行：

```sh
npm install
npm run dev
```

打开终端显示的本地网址。当前设备的依赖已在副本中准备好，可直接运行 `npm run dev`，也可双击“启动小游戏.cmd”。请勿直接双击 index.html，3D 场景需要本地服务。

## 玩法

1. 场景加载完成后自动开始 8 秒开场：“注意，他在偷看你！试着移动鼠标。”暂停或切换页面时不消耗这段时间。
2. 点击落下的爱心。命中会减轻压力、移除一颗已有堆积，并触发短暂反馈；1.25 秒内连续命中会显示连击。
3. 消除阶段开始前出现“小心，上面！点击清除。”失控程度达到上限且已进行至少 12 秒后，进入淹没剧情；最长 22 秒。
4. 淹没后，现有手套素材以呼吸动画指向铲子。直接点击整把铲子即可挖掘，热区随铲子位置及屏幕大小变化，并向外扩展 24 像素。
5. 每次挖掘清除 3 颗遮挡，5 次完成。第一次成功挖掘后手套淡出。无需拿取按钮，也无需再点击爱心堆。
6. 人物播放现有动画后，展示中性的本轮记录。“再来一次”直接重玩，不刷新页面。

当前只实现淹没后的这一条路线，零分也能完整体验。60 分以上的另一条故事线尚未编写，也没有替其设计结局。此次没有修改、重绘或修复模型和动画素材，僵尸式起身与欢呼的动作表现留待后续处理。

声音默认关闭，右上角可开启。点击“暂停”或按 Esc 可暂停；切换到其他页面时自动暂停，回来后点击继续。

## 改造内容

- 统一开场、阶段提示、压力条、计分、救援进度和结尾视觉。
- UI 根据三张参考图改为黑底、荧光粉紫的复古窗口：硬边框、横纹标题栏、分段进度条、底部对话框和轻微静态扫描线。中文保持可读，没有复制参考图中的文字或图案。
- 操作表现影响失控程度；开场及起身时隐藏计分面板，让人物成为画面重点。
- 爱心爆裂保留，新增命中文字、连击提示、可选合成音效和人物轻微反馈。
- 手套呼吸引导、整把铲子热区、直接点击挖掘。
- 资源就绪后自动开始 8 秒开场；加载失败提供刷新重试。
- 暂停、手机尺寸布局、触控操作和无需刷新的重玩。
- 正式界面隐藏原有调试面板，开发时可在网址后加 `?debug` 使用。

## 检查与构建

```sh
npm test
npm run build
npm run preview
```

`dist` 是构建结果。项目仍保留原有全部美术与模型，原始模型位于 `public/models`，默认运行使用 `public/models-mobile`。完整素材体积较大；构建中的 Three.js 文件大小提示不影响运行。

本版沿用原有角色模型及动画，没有重新建模。手机尺寸已做浏览器验证，真实手机的帧率仍取决于设备性能。
